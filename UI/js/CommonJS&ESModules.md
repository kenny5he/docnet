# CommonJS 
1. 基本用法
    1. 导出
    ```commonjs
    module.exports = {
    
    }
    ```
    2. 导入
    ```commonjs
    const path = require('path')
   ```
# ES Modules
1. 基本用法
    1. 导出:
    ```
    export default {
    
    }
   ```
    2. 导入:
    ```
    import path from 'path'
   ```
# Compile Plugin
## Babel (ES Module -> Common)

## Typescript (Typescript -> Common, ESModule )